# Tagless final demo in C++

This is a demo of tagless-final style in C++. It uses C++20 features to implement tagless-final style.

To run this demo, use the following command:

```bash
$ bazel run //cpp:main
```
